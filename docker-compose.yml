services:
  wynnbreeder:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - WYNNBREEDER_DB=/data/wynnbreeder.db
      - WYNNBREEDER_PORT=8080
    restart: unless-stopped
